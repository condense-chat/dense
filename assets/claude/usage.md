---
description: Claude subscription limits used, with condense vs without it
allowed-tools: Bash(dense usage:*), Bash(true)
---

!`dense usage 2>&1 || true`

Print the block above verbatim inside one fenced code block and nothing else.
Do not run the command again, do not summarize or explain it, and do not
offer follow-ups.
